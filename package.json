{
  "name": "linux-do-toolkit",
  "version": "1.1.0",
  "private": true,
  "description": "Linux.do 浏览器工具箱：Markdown 导出与阅读辅助",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "npm run build && node --check content.js && node --check popup.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8'))\""
  },
  "keywords": [
    "chrome-extension",
    "linux-do",
    "markdown",
    "discourse"
  ],
  "license": "UNLICENSED"
}
